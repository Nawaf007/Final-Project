@model StudentManagmentSystem.Models.SSExam

@{
    ViewBag.Title = "Delete";
}

<h2 style="text-align:center; font-weight:bold">Delete</h2>
<div>
    <h4 style="text-align:center; font-weight:bold">SSExam</h4>
    <h3>Are you sure you want to delete this?</h3>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Type
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Exam.Lookup.Values)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.SectionStudent.Student.RegistrationNo)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SectionStudent.Student.RegistrationNo)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.ObtainedMarks)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ObtainedMarks)
        </dd>

    </dl>

    @using (Html.BeginForm("Delete", "StudentSection", new { idsc = ViewBag.idsc, ids = ViewBag.ids, idc = ViewBag.idc, ide = ViewBag.ide, idss = ViewBag.idss }, FormMethod.Post, null))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index", new { idsc = ViewBag.idsc, ids = ViewBag.ids, idc = ViewBag.idc, ide = ViewBag.ide, idss = ViewBag.idss })
        </div>
    }
</div>
